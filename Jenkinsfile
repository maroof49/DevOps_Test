pipeline {
  agent any
  maven 'mvn'

  stages{
    stage ('build the code'){
      mvn install 
    }

    stage ('build the docker file){
           
    }

    stage ('push the docker file){
      
    }
  }
}
